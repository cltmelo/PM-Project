1. fitness.py: Python compatibility bug — X | None syntax in function signatures: The return type tuple[bool, FrozenSet[str] | None] and parameter type FrozenSet[str] | None use the union | operator in annotations, which only works at runtime in Python 3.10+. On Python 3.9 or earlier this raises a TypeError when the module is imported (It is possible to fix it with a single prompt).

2. 